name: voicebot-rag
metadata:
  template: voicebot-rag@0.1.0
services:
  bridge:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./bridge/Dockerfile
      context: .
      remoteBuild: true
